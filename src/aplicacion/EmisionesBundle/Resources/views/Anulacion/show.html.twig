{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Anulacion</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Numeroorden</th>
                <td>{{ entity.numeroOrden }}</td>
            </tr>
            <tr>
                <th>Datosboleto</th>
                <td>{{ entity.datosBoleto }}</td>
            </tr>
            <tr>
                <th>Vtc</th>
                <td>{{ entity.vtc }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('anulacion') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('anulacion_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
