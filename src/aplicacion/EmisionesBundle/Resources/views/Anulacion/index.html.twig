{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Anulacion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numeroorden</th>
                <th>Datosboleto</th>
                <th>Vtc</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('anulacion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.numeroOrden }}</td>
                <td>{{ entity.datosBoleto }}</td>
                <td>{{ entity.vtc }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('anulacion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('anulacion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('anulacion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
