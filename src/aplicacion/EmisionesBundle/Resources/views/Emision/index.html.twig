{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Emision list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Reservapnr</th>
                <th>Tarifareserva</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('emision_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.reservaPnr }}</td>
                <td>{{ entity.tarifaReserva }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('emision_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('emision_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('emision_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
