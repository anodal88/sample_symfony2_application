{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Orden list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fecha</th>
                <th>Tipoboleto</th>
                <th>Recordgds</th>
                <th>Tourcode</th>
                <th>Feeservicios</th>
                <th>Observaciones</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('orden_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.tipoBoleto }}</td>
                <td>{{ entity.recordGds }}</td>
                <td>{{ entity.tourcode }}</td>
                <td>{{ entity.feeServicios }}</td>
                <td>{{ entity.observaciones }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('orden_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('orden_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('orden_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
