{% extends '::base.html.twig' %}

{% block body -%}
    <h1>DepefectivoTransferenciabancaria list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Banco</th>
                <th>Numerodocumento</th>
                <th>Valor</th>
                <th>Transaccion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('depefectivotransferenciabancaria_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.banco }}</td>
                <td>{{ entity.numeroDocumento }}</td>
                <td>{{ entity.valor }}</td>
                <td>{{ entity.transaccion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('depefectivotransferenciabancaria_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('depefectivotransferenciabancaria_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('depefectivotransferenciabancaria_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
